pipeline {
    agent any 

    stages {
        stage('Clone Repo') {
            steps {
                checkout scm

                sh 'ls -la'
                sh 'env'
            }
        }

        stage('Identify Changes') {
            steps {
                sh 'env'
            }
        }
    }
}
